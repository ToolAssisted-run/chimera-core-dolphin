diff --git a/Source/Core/VideoBackends/Software/SWGfx.cpp b/Source/Core/VideoBackends/Software/SWGfx.cpp
index 7004c50864..5c83f5aa22 100644
--- a/Source/Core/VideoBackends/Software/SWGfx.cpp
+++ b/Source/Core/VideoBackends/Software/SWGfx.cpp
@@ -26,7 +26,7 @@ SWGfx::~SWGfx() = default;
 
 bool SWGfx::IsHeadless() const
 {
-  return m_window->IsHeadless();
+  return !m_window || m_window->IsHeadless();
 }
 
 bool SWGfx::SupportsUtilityDrawing() const
@@ -58,7 +58,7 @@ SWGfx::CreateFramebuffer(AbstractTexture* color_attachment, AbstractTexture* dep
 bool SWGfx::BindBackbuffer(const ClearColor& clear_color)
 {
   // Look for framebuffer resizes
-  if (!g_presenter->SurfaceResizedTestAndClear())
+  if (!m_window || !g_presenter->SurfaceResizedTestAndClear())
     return true;
 
   GLContext* context = m_window->GetContext();
@@ -137,6 +137,8 @@ void SWGfx::SetScissorRect(const MathUtil::Rectangle<int>& rc)
 
 SurfaceInfo SWGfx::GetSurfaceInfo() const
 {
+  if (!m_window)
+    return {640, 528, 1.0f, AbstractTextureFormat::RGBA8};
   GLContext* context = m_window->GetContext();
   return {std::max(context->GetBackBufferWidth(), 1u), std::max(context->GetBackBufferHeight(), 1u),
           1.0f, AbstractTextureFormat::RGBA8};
diff --git a/Source/Core/VideoBackends/Software/SWmain.cpp b/Source/Core/VideoBackends/Software/SWmain.cpp
index 66c96f5a16..126343c23d 100644
--- a/Source/Core/VideoBackends/Software/SWmain.cpp
+++ b/Source/Core/VideoBackends/Software/SWmain.cpp
@@ -91,9 +91,15 @@ void VideoSoftware::InitBackendInfo(const WindowSystemInfo& wsi)
 
 bool VideoSoftware::Initialize(const WindowSystemInfo& wsi)
 {
-  std::unique_ptr<SWOGLWindow> window = SWOGLWindow::Create(wsi);
-  if (!window)
-    return false;
+  // A headless machine rasterizes without presenting; no GL context exists
+  // or is needed. Every m_window use in SWGfx honours the null.
+  std::unique_ptr<SWOGLWindow> window;
+  if (wsi.type != WindowSystemType::Headless)
+  {
+    window = SWOGLWindow::Create(wsi);
+    if (!window)
+      return false;
+  }
 
   Clipper::Init();
   Rasterizer::Init();
diff --git a/Source/Core/VideoCommon/VideoBackendBase.cpp b/Source/Core/VideoCommon/VideoBackendBase.cpp
index 7eeefb7187..b0ac766b5b 100644
--- a/Source/Core/VideoCommon/VideoBackendBase.cpp
+++ b/Source/Core/VideoCommon/VideoBackendBase.cpp
@@ -31,9 +31,9 @@
 #include "VideoBackends/D3D12/VideoBackend.h"
 #endif
 #include "VideoBackends/Null/VideoBackend.h"
+#include "VideoBackends/Software/VideoBackend.h"
 #ifdef HAS_OPENGL
 #include "VideoBackends/OGL/VideoBackend.h"
-#include "VideoBackends/Software/VideoBackend.h"
 #endif
 #ifdef HAS_VULKAN
 #include "VideoBackends/Vulkan/VideoBackend.h"
@@ -223,9 +223,9 @@ const std::vector<std::unique_ptr<VideoBackendBase>>& VideoBackendBase::GetAvail
 #ifdef __APPLE__
     backends.emplace(backends.begin(), std::make_unique<Metal::VideoBackend>());
 #endif
-#ifdef HAS_OPENGL
+    // The software renderer rasterizes without any GL; only its optional
+    // presentation window ever needed one.
     backends.push_back(std::make_unique<SW::VideoSoftware>());
-#endif
     backends.push_back(std::make_unique<Null::VideoBackend>());
 
     if (!backends.empty())
