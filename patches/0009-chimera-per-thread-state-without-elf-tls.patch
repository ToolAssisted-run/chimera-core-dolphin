diff --git a/Source/Core/Common/SandboxTls.h b/Source/Core/Common/SandboxTls.h
new file mode 100644
index 0000000000..da015e1944
--- /dev/null
+++ b/Source/Core/Common/SandboxTls.h
@@ -0,0 +1,31 @@
+// Copyright 2026 Dolphin Emulator Project
+// SPDX-License-Identifier: GPL-2.0-or-later
+
+// For hosts without ELF TLS (CHIMERA_NO_TLS): a pthread key wearing a value's
+// clothes. Zero-initialized per thread, like the thread_local it replaces.
+#pragma once
+
+#include <pthread.h>
+#include <cstdint>
+
+namespace Common
+{
+template <typename T>
+class SandboxTlsValue
+{
+public:
+  SandboxTlsValue() { pthread_key_create(&m_key, nullptr); }
+  SandboxTlsValue& operator=(T v)
+  {
+    pthread_setspecific(m_key, reinterpret_cast<void*>(static_cast<uintptr_t>(v)));
+    return *this;
+  }
+  operator T() const
+  {
+    return static_cast<T>(reinterpret_cast<uintptr_t>(pthread_getspecific(m_key)));
+  }
+
+private:
+  pthread_key_t m_key;
+};
+}  // namespace Common
diff --git a/Source/Core/Core/Core.cpp b/Source/Core/Core/Core.cpp
index 8ce66e67bb..4879ffa38f 100644
--- a/Source/Core/Core/Core.cpp
+++ b/Source/Core/Core/Core.cpp
@@ -126,8 +126,14 @@ struct HostJob
 static std::mutex s_host_jobs_lock;
 static std::queue<HostJob> s_host_jobs_queue;
 
+#ifdef CHIMERA_NO_TLS
+#include "Common/SandboxTls.h"
+static Common::SandboxTlsValue<bool> tls_is_cpu_thread;
+static Common::SandboxTlsValue<bool> tls_is_gpu_thread;
+#else
 static thread_local bool tls_is_cpu_thread = false;
 static thread_local bool tls_is_gpu_thread = false;
+#endif
 
 static void EmuThread(Core::System& system, std::unique_ptr<BootParameters> boot,
                       WindowSystemInfo wsi);
diff --git a/Source/Core/InputCommon/ControllerInterface/ControllerInterface.cpp b/Source/Core/InputCommon/ControllerInterface/ControllerInterface.cpp
index 20fd517b2d..9d7087c06c 100644
--- a/Source/Core/InputCommon/ControllerInterface/ControllerInterface.cpp
+++ b/Source/Core/InputCommon/ControllerInterface/ControllerInterface.cpp
@@ -43,9 +43,15 @@ ControllerInterface g_controller_interface;
 // update values in different threads by input channel. We start from InputChannel::Host on all
 // threads as hotkeys are updated from a worker thread, but UI can read from the main thread. This
 // will never interfere with game threads.
-static thread_local ciface::InputChannel tls_input_channel = ciface::InputChannel::Host;
+#ifdef CHIMERA_NO_TLS
+#include "Common/SandboxTls.h"
+static Common::SandboxTlsValue<ciface::InputChannel> tls_input_channel;
 
+static Common::SandboxTlsValue<bool> tls_is_updating_devices;
+#else
+static thread_local ciface::InputChannel tls_input_channel = ciface::InputChannel::Host;
 static thread_local bool tls_is_updating_devices = false;
+#endif
 
 void ControllerInterface::Initialize(const WindowSystemInfo& wsi)
 {
