# Committed test content

- `swiss_r2092.dol` - [Swiss](https://github.com/emukidid/swiss-gc) r2092,
  GPL-2.0, built by its authors and redistributed unmodified from their
  release archive (swiss_r2092.tar.xz). The gate boots it because a GameCube
  needs no firmware to run a homebrew executable.

User-supplied commercial content lives in `../roms-local/` (gitignored,
never committed).
